language: en

pipeline:
- name: WhitespaceTokenizer
- name: rasa_nlu_examples.featurizers.sparse.HashingFeaturizer
  norm: null  # do not normalize so the counts of token and sentence features can be compared in the unit test
  n_features: 1024
- name: KeywordIntentClassifier
  case_sensitive: True
- name: rasa_nlu_examples.meta.Printer
  alias: After intent classifier
